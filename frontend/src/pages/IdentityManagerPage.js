import React from 'react';
import IdentityManager from '../components/Identity/IdentityManager';

const IdentityManagerPage = () => {
    return <IdentityManager />;
};

export default IdentityManagerPage;